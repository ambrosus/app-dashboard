import { LoopExcludePipe } from './loop-exclude.pipe';

describe('LoopExcludePipe', () => {
  it('create an instance', () => {
    const pipe = new LoopExcludePipe();
    expect(pipe).toBeTruthy();
  });
});
